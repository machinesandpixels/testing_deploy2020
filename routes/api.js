const express = require('express');
const router = express.Router();
// const ctrl = require('../controllers');

// -------- Order Routes
// router.get('/order', ctrl.orders.index);
// router.post('/purchase', ctrl.orders.create);

module.exports = router;